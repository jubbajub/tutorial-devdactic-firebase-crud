import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'tutorial-devdactic-firebase-crud',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
